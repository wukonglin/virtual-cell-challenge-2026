# Source-bound GenePT conditioning v11

Feature-statistic preparation only. No expression reads, predictive model
training, GPU, network, W&B upload, submission or automatic training admission.

Pin the completed numeric GenePT artifact with SHA256
`f1c29e064be43a6a2ebfc03371fb23537edf401582e8d5e2187967ebd3b83536`
and its conversion receipt
`f565eb23256722ed3b85d5960a71086138670aff55d58d2c2660dc8fe23cdc57`.
Before decoding it, register all787 names, unchanged v10 public source roles,
all98 globally held identities,300 official names, source-code hashes, this
protocol, seeds20260926/20260927 and the following duplicate quarantine.

Keep the immutable raw artifact. In a copied feature table, quarantine both
names of each exact duplicate pair:BRD1/BRPF1,LTB4R2/NOP9,MBD4/MED1,SKP2/UXT.
Zero their GenePT vectors and set availability0 and source-object ID-1, without
changing target identities, GO or response-role eligibility. There must be
774available rows afterward and no remaining cross-role exact vector sharing.
These pairs are representation ambiguities, not proven gene synonyms.

For each source separately, fit coordinate-wise means plus one scalar RMS
over available fitting targets only. Explicitly reject fitting/held/official
identity overlap. The five originally absent and eight quarantined rows do
not participate in these statistics; their transformed vectors remain zero.
Store the transform, exact fitting identities, availability, statistics hashes
and its application to all787 names. No official or held row fits a statistic.

For source-local public target names only, materialize correct, masked and
shuffled GenePT arms for each seed. All arms retain identical availability
flags, exact target order and unchanged separate GO input. The shuffled arm
deranges vectors independently within fitting/held and availability strata,
with separate RNG streams. Explicitly report singleton/unchanged strata.
The masked vector is not a non-targeting perturbation indicator.

Replay every transform and numeric NPZ using allow_pickle=False; verify fitted
available feature means near0 and scalar mean-square near1. Source arrays for
future unseen-target/source inference reuse these frozen transforms.

Bounds:approved BioHPC compute host only, no head/login; GPUs hidden;2CPU cores
and threads;120seconds;3GiB address space,2GiB peakRSS,128MiB cumulative output.
Fresh non-overwriting directories; verify provenance again before completion.
No response, optimizer or predictive-model checkpoint is created by this step.
