# Training-only overlay for the pinned arc-state 0.11.3 checkout.
#
# Install requirements/model.txt first, then this file with model.txt as a
# constraint, and finally install external/state_runtime_9bbfe78a with
# `pip install --no-deps -e`.  cell-eval is deliberately absent: its current
# release requires anndata>=0.12.10, while the registered model runtime pins
# anndata==0.11.4.  Official scoring belongs in the separate scoring runtime.

cell-load==0.10.4
geomloss==0.2.6
hydra-core==1.3.2
ipykernel==6.30.1
lightning==2.5.2
peft==0.11.0
pytorch-lightning==2.5.2
PyYAML==6.0.3
scanpy==1.11.2
seaborn==0.13.2
toml==0.10.2
tqdm==4.67.1
transformers==4.52.3
wandb==0.19.11
